import Grid from "@mui/material/Grid";
import { useSelector } from "react-redux";

export const EventListBar: React.FC = () => {
  //const a = useSelector(getEventL)
  return <Grid container></Grid>;
};
